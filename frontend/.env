# VITE_API_URL=http://localhost:4000
VITE_API_URL=http://202.93.8.4/inventory
# VITE_API_URL=http://202.93.8.4/inventories

# Direct backend URL for Socket.IO (bypasses nginx reverse-proxy).
# Required in production when the backend sits behind a path-based proxy (e.g. /inventory).
# Set to the backend container/port that is directly reachable from the browser.ssssss
VITE_SOCKET_URL=http://202.93.8.4:4000
